# 数学乐园 — 测试文档

## 1. 测试策略

由于本项目是纯前端单 HTML 文件，测试分为两类：
- **自动化测试**：通过内嵌测试脚本，在浏览器控制台中运行，验证核心逻辑（题目生成、状态管理）
- **手动测试**：验证 UI 交互、音频、动画、响应式布局等

---

## 2. 自动化测试用例（题目生成算法）

以下测试可通过浏览器控制台运行，或在 HTML 中内嵌 `<script>` 测试块实现。

### TC-01: 加法题生成 — 结果在 [0, max] 范围内
```
对每种模式 (within20/50/100) 各生成 500 道加法题
断言：每道题的 answer ∈ [0, max]
断言：每道题的所有 operands ≥ 3（过滤极简题）
断言：operands[0] + operands[1] === answer
```

### TC-02: 减法题生成 — 结果非负且不过于简单
```
对每种模式各生成 500 道减法题
断言：每道题的 answer ≥ 0 且 ≤ max
断言：operands[0] ≥ 5（被减数不过小）
断言：operands[1] ≥ 2（减数不过小）
断言：operands[0] - operands[1] === answer
```

### TC-02b: 进位加法生成
```
断言：answer ∈ [0, max]
断言：计算正确
```

### TC-02c: 退位减法生成
```
断言：answer ∈ [0, max]
断言：计算正确
```

### TC-03: 连加连减题生成（2运算符）— 中间结果和最终结果合法
```
对每种模式各生成 500 道连加连减题
断言：每道题有 3 个 operands 和 2 个 operators
断言：各操作数 ≥ 2（过滤极简）
断言：中间结果 mid ∈ [0, max]
断言：最终结果 answer ∈ [0, max]
断言：按运算符顺序计算等于 answer
```

### TC-03b: 长连加连减题生成（3运算符）
```
断言：每道题有 4 个 operands 和 3 个 operators
断言：所有中间结果和最终结果 ∈ [0, max]
断言：按运算符顺序计算等于 answer
```

### TC-04: 关卡题目无重复
```
生成一关 20 道题
断言：所有题目的 operands+operators 组合唯一
```

### TC-05: 题型分布合理
```
对每种模式各生成 5 关共 100 道题
统计加减法和连加连减各自比例
断言：连加连减（含2-op和3-op）总占比 ≥ 25%
```

### TC-06: 答案分布不极端集中
```
生成一关 20 道题
将答案范围均分为若干区间（如 20 以内分 4 区：0-5/6-10/11-15/16-20）
断言：单个区间内的答案数量不超过总题数的 40%（即不超过 8 道）
```

### TC-07: 操作数不过小（过滤极简题目）
```
对每种模式各生成 500 道题
断言：加法题两个操作数均 ≥ 3
断言：减法题被减数 ≥ 5，减数 ≥ 2
断言：连加连减题各操作数 ≥ 2
```

### TC-08: 操作数不含 0
```
对每种模式各生成 500 道题
断言：所有题目的所有操作数均 > 0
```

### TC-09: 答案为 0 的题目限制
```
对每种模式各生成 10 关（共 200 道题/模式）
断言：每关答案为 0 的题目数量 ≤ 1
```

### TC-10: 简单题比例控制
```
对每种模式各生成 5 关共 100 道题
定义"简单题"：加法/减法中所有操作数均 < 模式max的25%
断言：简单题占比 ≤ 25%
```

---

## 3. 功能测试用例（手动测试）

### 3.1 开始页面

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-01 | 打开 index.html | 显示开始页面，有标题"数学乐园"、小奶龙打招呼图片(nailong-sayhi.png)、三个难度按钮 |
| MT-01a | 页面加载完成 | WorldSceneBGM 尝试自动播放；若被浏览器阻止，点击页面任意位置后开始播放 |
| MT-02 | 点击"20以内加减法" | 跳转到游戏页面，显示第1关第1题 |
| MT-02b | 点击"50以内加减法" | 跳转到游戏页面，题目范围在50以内 |
| MT-02c | 点击"100以内加减法" | 跳转到游戏页面，题目范围在100以内，可输入3位数 |

### 3.2 答题流程

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-04 | 查看游戏页面 | 顶栏显示"第1关"、进度环(1/20)、倒计时 |
| MT-05 | 点击数字键 | 答案显示区实时显示输入的数字 |
| MT-06 | 点击删除键 | 删除最后一位数字 |
| MT-07 | 输入超过2位数字 | 不响应第3次输入 |
| MT-08 | 不输入直接按确认 | 无响应（不提交空答案） |
| MT-08a | 输入位数达到当前题正确答案位数 | 自动提交并判题（无需点确认） |
| MT-08b | 自动提交后立即再点确认 | 不会重复提交或跳题 |
| MT-09 | 输入正确答案并确认 | 播放答对音效+TTS鼓励、小奶龙好奇图(nailong-curious.png)、彩纸飘落、1.5秒后下一题 |
| MT-10 | 输入错误答案并确认（第1次） | 播放答错音效+TTS鼓励重试、小奶龙不开心图(nailong-unhappy.png)、题目抖动、倒计时重置、输入清空 |
| MT-11 | 第2次答错确认 | 显示正确答案、播放安慰语音、2秒后下一题 |
| MT-12 | 等待倒计时到0 | 视为答错，进入答错流程 |

### 3.3 倒计时

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-13 | 新题出现 | 倒计时从15秒开始 |
| MT-14 | 观察倒计时条 | 平滑递减，绿→黄→红颜色变化 |
| MT-15 | 剩余3秒时 | 倒计时条变红并闪烁 |
| MT-16 | 答错重试时 | 倒计时重置回15秒 |
| MT-17 | 答对时 | 倒计时停止 |

### 3.4 关卡结算

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-18 | 完成第1关20题 | 显示关卡结算页：正确率、平均用时、小奶龙开心图(nailong-happy.jpg) |
| MT-18a | 本关存在答错的题目（含重试后答对的） | 结算页显示”查看错题”按钮，与”继续下一关”/”休息一下”同级 |
| MT-18b | 本关全对（从未答错）进入结算页 | 不显示”查看错题”按钮，或按钮置灰 |
| MT-18c | 点击”查看错题”按钮 | 进入独立全屏错题回顾页，显示所有第一次答错的题目 |
| MT-18d | 第一次答错、第二次答对的题 | 在错题回顾中出现，显示第一次的错误答案和正确答案 |
| MT-18e | 在错题回顾页点击”返回” | 返回关卡结算页 |
| MT-19 | 点击”继续下一关” | 进入第2关第1题 |
| MT-20 | 点击"休息一下" | 进入休息页面 |
| MT-21 | 在休息页点击"准备好了" | 进入下一关第1题 |
| MT-22 | 完成第4关 | 显示全部完成页：总正确率、总用时 |
| MT-23 | 点击"再玩一次" | 回到开始页面 |

### 3.4A 盲盒奖励

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-23a | 完成任意一关20题 | 先进入盲盒页，再进入关卡结算页 |
| MT-23b | 观察盲盒方块 | 方块微微周期性抽动（吸引点击） |
| MT-23c | 点击盲盒方块（? 区域） | 触发抽奖（与点击”开始抽奖”按钮效果相同） |
| MT-23d | 点击”开始抽奖”按钮 | 盲盒方块先剧烈摇晃，然后 emoji 轮转，0.8~1.2s 后揭晓结果 |
| MT-23e | 观察 emoji 轮转 | 轮转图标丰富多样（10+ 种），且每次顺序不同 |
| MT-23f | 抽中后查看结果区域 | 直接显示鼓励语（如”数学小能手！””每天进步一点点！”），不显示”抽中：XXX”文字，不同品质鼓励语不同 |
| MT-23g | 连续完成4关并记录奖励 | 每关仅抽1次，奖励结果有随机性 |
| MT-23h | 检查同一局相邻两关奖励 | 尽量不出现完全相同组合（弱去重） |
| MT-23i | 抽到超稀有奖励 | 揭晓音效为 `unbelievable.mp3`，鼓励语更兴奋 |

### 3.5 进度展示

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-24 | 完成第1题 | 进度环从 1/20 变为 2/20 |
| MT-25 | 完成第10题 | 进度环填充约 50% |
| MT-26 | 完成第20题 | 进度环填满 |

### 3.6 音效和语音

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-27 | 页面加载完成 | 尝试自动播放 WorldSceneBGM；若浏览器阻止则首次点击后播放 |
| MT-28 | 答对5次 | 优先播放本地鼓励音频；缺失时回退 TTS 女声 |
| MT-29 | 点击数字键 | 播放 `click.bubble.mp3` 点击音 |
| MT-29a | 点击确认/继续/休息/准备好了 | 播放 `click.common.button.mp3` |
| MT-29b | 答错首次 | 播放 `board.mp3` |
| MT-29c | 答错最终或二次超时 | 播放 `ice_break.mp3` |
| MT-29d | 进入答题页/离开答题页 | BGM 在 `GameSceneBGM.mp3` 与 `WorldSceneBGM.mp3` 间切换 |
| MT-30 | 点击音频开关按钮 | 背景音乐和音效静音/恢复 |

### 3.7 小奶龙角色

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-31 | 首页查看角色 | 显示小奶龙打招呼图片(nailong-sayhi.png)，图片轻轻上下浮动 |
| MT-32 | 答对时 | 显示小奶龙好奇图片(nailong-curious.png) |
| MT-33 | 答错时 | 显示小奶龙不开心图片(nailong-unhappy.png) |
| MT-34 | 关卡完成时 | 显示小奶龙开心图片(nailong-happy.jpg) |
| MT-35 | 游戏全部完成时 | 显示小奶龙闪亮得意图片(nailong-twinkle.png) |
| MT-36 | 所有场景的图片加载 | 图片正常加载无破图，大小自适应，保持比例 |

### 3.8 整体配色

| 编号 | 测试步骤 | 预期结果 |
|------|---------|---------|
| MT-37 | 查看整体色系 | 黄色系暖色调配色，呼应小奶龙黄白配色 |
| MT-38 | 检查文字可读性 | 所有文字在背景上清晰可读，对比度足够 |
| MT-39 | 检查按钮可读性 | 按钮文字与按钮背景色对比度足够，不同状态(默认/选中/禁用)均清晰可辨 |

---

## 4. 边界条件测试

| 编号 | 场景 | 预期结果 |
|------|------|---------|
| BT-01 | 答案为 0（如 5-5=0） | 每关最多1道，可以输入"0"并正确提交 |
| BT-02 | 答案为 20（如 11+9=20） | 可以输入"20"并正确提交 |
| BT-03 | 快速连续点击确认 | 不会重复提交或跳题 |
| BT-03a | 自动判题刚触发时再次输入数字 | 输入被忽略，不发生二次提交 |
| BT-04 | 答案展示期间点击键盘 | 键盘无响应（锁定状态） |
| BT-05 | 倒计时最后1秒提交正确答案 | 正常判定为答对 |
| BT-06 | 连续20题全答对 | 关卡结算显示100%正确率 |
| BT-07 | 连续20题全答错（共40次提交） | 正常记录，不崩溃 |
| BT-08 | 完成全部4关80题 | 总结算页面正常显示，点击再玩一次可重置 |
| BT-09 | 音频文件路径缺失 | 游戏流程不阻塞，回退 TTS 或静默继续 |
| BT-10 | 同一关产生多次错误后检查错题回顾 | 错题列表包含所有第一次答错的题目（含重试后答对的），数量正确 |
| BT-11 | 所有题目的操作数 | 均不为 0（不出现加/减 0 的题目） |
| BT-12 | 查看一关 20 题的答案分布 | 答案分散在多个区间，无极端集中 |

---

## 5. 响应式布局测试

| 编号 | 设备/视口 | 测试内容 | 预期结果 |
|------|----------|---------|---------|
| RT-01 | iPhone SE (375×667) | 所有页面 | 布局正常，按钮可点击，无溢出 |
| RT-02 | iPhone 14 (390×844) | 所有页面 | 布局正常 |
| RT-03 | iPad Mini (768×1024) | 所有页面 | 居中显示，按钮更大 |
| RT-04 | iPad Pro (1024×1366) | 所有页面 | 居中显示，内容不拉伸过宽 |
| RT-05 | 横屏模式 | 游戏页面 | 布局自适应，不遮挡关键元素 |

---

## 6. 性能测试

| 编号 | 测试内容 | 预期结果 |
|------|---------|---------|
| PT-01 | 页面首次加载时间 | < 1秒（本地文件） |
| PT-02 | 连续完成80题 | 无明显卡顿，内存稳定 |
| PT-03 | Confetti 动画执行后 | DOM 节点被清理，无累积 |
| PT-04 | 长时间运行（30分钟） | 无内存泄漏 |

---

## 7. 自动化测试脚本

在 `index.html` 中内嵌以下测试函数，可在浏览器控制台通过 `runTests()` 执行：

```javascript
function runTests() {
  let passed = 0, failed = 0;

  function assert(condition, msg) {
    if (condition) { passed++; }
    else { failed++; console.error('FAIL:', msg); }
  }

  // TC-01 ~ TC-07 的实现
  // ...

  console.log(`Tests: ${passed} passed, ${failed} failed`);
  return failed === 0;
}
```

详细实现见 index.html 底部的测试代码区域。

---

## 8. 测试通过标准

- 所有自动化测试用例 (TC-01 ~ TC-10) 通过
- 所有 P0 功能手动测试用例通过
- 所有边界条件测试用例通过
- 手机和平板响应式布局正常
- 无控制台 JavaScript 错误
